function sunburst() {
    var width = 960,
        height = 700,
        radius = Math.min(width, height) / 2,
        color = d3.scale.category20c();

    function my() {
        // generate chart here
    }
}